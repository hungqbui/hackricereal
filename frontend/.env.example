# Where the FastAPI backend is listening. The dev server proxies /api here.
VITE_BACKEND_URL=http://127.0.0.1:8000

# Only needed for a production build served from a different origin than the
# API. Leave unset in development so the proxy above is used.
# VITE_API_BASE=https://api.example.com
